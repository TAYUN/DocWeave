# Infrastructure

本目录只承载本地开发阶段需要接入的基础设施入口，方便后续为各运行时补充容器、脚本或初始化说明。

## Included Services

- `postgres`: 本地数据库入口
- `redis`: 本地缓存与队列依赖入口
- `qdrant`: 本地向量检索依赖入口
- `minio`: 本地对象存储依赖入口

## Boundary

- 这里只表达本地开发依赖的目录基线
- 当前不承诺生产级部署编排、监控、备份或高可用方案
